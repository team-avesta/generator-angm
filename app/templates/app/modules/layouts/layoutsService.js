(function() {
    'use strict';

    angular
        .module('layouts')
        .factory('layoutsService', service);

    service.$inject = ['$http'];

    function service($http) {
        var service = {};

        return service;

        ////////////
        ///start writing function implementations below
    }
})();
